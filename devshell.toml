# https://numtide.github.io/devshell
[[commands]]
help = "Rebuilds nixos system."
name = "rebuild"
command = "alejandra * -q && git add . && nh os switch"
[[commands]]
help = "Update and rebuild the nixos system"
name = "upbuild"
command = "alejandra * -q && git add . && nh os switch --update"
